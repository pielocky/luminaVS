# Основные зависимости
kivy==2.1.0
opencv-python==4.8.1.78
numpy==1.24.3
easyocr==1.7.1
pillow==10.0.0
pyzbar==0.1.9
gtts==2.3.2

# Аудио воспроизведение (выберите один вариант)
playsound==1.3.0

# Для Android сборки дополнительные зависимости
# (эти строки закомментированы, так как они для buildozer.spec)
# ------------------------------------------------------------
# android: requirements = python3,kivy,opencv,numpy,android,numpy,pyjnius,openssl,ffmpeg
# android: ndk = 25b
# android: sdk = 33
# ------------------------------------------------------------

# Для Windows/Mac/Linux разработки дополнительные полезные пакеты
# (опционально, для тестирования)
pytest==7.4.0  # для тестирования